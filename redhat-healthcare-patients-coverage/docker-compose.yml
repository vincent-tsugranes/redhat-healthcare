version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: healthcare-coverage-postgres
    environment:
      POSTGRES_DB: healthcare_coverage
      POSTGRES_USER: healthcare
      POSTGRES_PASSWORD: healthcare
    ports:
      - "5433:5432"
    volumes:
      - coverage_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U healthcare"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  coverage_postgres_data:
